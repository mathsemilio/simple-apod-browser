<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.mathsemilio.simpleapodbrowser">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".SimpleAPoDBrowserApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher_simple_apod_browser"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_simple_apod_browser_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SimpleAPoDBrowser">
        <activity
            android:name=".ui.MainActivity"
            android:configChanges="orientation|screenLayout|screenSize|keyboardHidden"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>